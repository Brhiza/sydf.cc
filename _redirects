# SPA 路由支持 - 所有非文件路径都重定向到 index.html
/*    /index.html   200

# 强制 HTTPS 重定向
http://*    https://*:443    301

# 移除尾部斜杠（可选）
/*    /:splat    301

# 旧路径重定向（如果有历史路径需要保持）
# /old-path    /new-path    301
