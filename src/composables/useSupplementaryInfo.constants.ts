import { EARTHLY_BRANCHES, HEAVENLY_STEMS } from '@/shared/supplementary-info';

export const heavenlyStems = HEAVENLY_STEMS.map((name) => ({ name, remark: '' }));

export const earthlyBranches = EARTHLY_BRANCHES.map((name) => ({ name, remark: '' }));
